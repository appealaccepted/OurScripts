local library = loadstring(game:GetObjects("rbxassetid://7657867786")[1].Source)()
local Wait = library.subs.Wait -- Only returns if the GUI has not been terminated. For 'while Wait() do' loops

local PepsisWorld = library:CreateWindow({
Name = "Philly Streetz",
Themeable = {
Info = "Discord Server: VzYTJ7Y"
}
})

local MainTab = PepsisWorld:CreateTab({
Name = "Main"
})

local FarmingSection = MainTab:CreateSection({
    Name = "Game"
 })


 
 FarmingSection:AddButton({
    Name = "Inf Stamina",
    Callback = function()
        local remote = game.Players.LocalPlayer.Events:FindFirstChild("Energy")

        if remote then
            remote:Destroy() -- Deletes the RemoteEvent
            print("RemoteEvent deleted successfully.")
        else
            print("RemoteEvent not found.")
        end
        
    end
 })


 FarmingSection:AddButton({
   Name = "Fix Running",
   Callback = function()
      -- Define the parent where the RemoteEvent should be placed
local parent = game.Players.LocalPlayer.Events -- Change this to the appropriate service

-- Check if the RemoteEvent already exists to avoid duplicates
if not parent:FindFirstChild("MyRemoteEvent") then
    -- Create a new RemoteEvent
    local remoteEvent = Instance.new("RemoteEvent")
    remoteEvent.Name = "Energy" -- Replace with the original name
    remoteEvent.Parent = parent
    print("RemoteEvent 'MyRemoteEvent' has been recreated in " .. parent.Name)
else
    print("RemoteEvent 'MyRemoteEvent' already exists in " .. parent.Name)
end
    
   end
})



 FarmingSection:AddButton({
    Name = "Instant ProximityPrompts",
    Callback = function()
        local function ProximityPromptDuration(HoldDur)
            for i,v in workspace:GetDescendants() do
                 if v:IsA("ProximityPrompt") then
                     v.HoldDuration = HoldDur
                 end
            end
         end
 
         ProximityPromptDuration(0)
 
        workspace.DescendantAdded:Connect(function(Added)
             if Added:IsA("ProximityPrompt") then
                 ProximityPromptDuration(0)
             end
        end)
        
    end
 })

 local VisualsSection = MainTab:CreateSection({
    Name = "Visuals" 
 })

 VisualsSection:AddButton({
   Name = "Skelly",
   Callback = function()
      local Players = game:GetService("Players")
		local RunService = game:GetService("RunService")
		local Camera = workspace.CurrentCamera
		local UserInputService = game:GetService("UserInputService")
		
		local skeletonEnabled = false -- ESP starts off
		local playerLines = {} -- Stores drawing lines for all players
		local playerTags = {} -- Stores name tags for each player
		
		-- Body parts to connect for skeleton
		local bodyParts = {
			{ "Head", "UpperTorso" },
			{ "UpperTorso", "LowerTorso" },
			{ "LeftUpperArm", "LeftLowerArm" },
			{ "LeftLowerArm", "LeftHand" },
			{ "RightUpperArm", "RightLowerArm" },
			{ "RightLowerArm", "RightHand" },
			{ "LeftUpperLeg", "LeftLowerLeg" },
			{ "LeftLowerLeg", "LeftFoot" },
			{ "RightUpperLeg", "RightLowerLeg" },
			{ "RightLowerLeg", "RightFoot" },
			{ "UpperTorso", "LeftUpperArm" },
			{ "UpperTorso", "RightUpperArm" },
			{ "LowerTorso", "LeftUpperLeg" },
			{ "LowerTorso", "RightUpperLeg" },
		}
		
		-- Function to create a line using the Drawing API
		local function createLine()
			local line = Drawing.new("Line")
			line.Thickness = 1.5
			line.Color = Color3.new(1, 1, 1) -- White
			line.Transparency = 1
			line.Visible = false
			return line
		end
		
		-- Function to create a name tag for each player
		local function createNameTag()
			local nameTag = Drawing.new("Text")
			nameTag.Size = 14 -- Adjust the size to be readable
			nameTag.Color = Color3.new(1, 1, 1) -- White
			nameTag.Transparency = 1
			nameTag.Center = true -- Centers the text above the player's head
			nameTag.Outline = true -- Adds an outline for better visibility
			nameTag.Visible = false
			return nameTag
		end
		
		-- Toggle ESP visibility
		local function toggleESP()
			skeletonEnabled = not skeletonEnabled
			if not skeletonEnabled then
				-- Hide all lines and tags when ESP is disabled
				for _, lines in pairs(playerLines) do
					for _, line in pairs(lines) do
						line.Visible = false
					end
				end
				for _, nameTag in pairs(playerTags) do
					nameTag.Visible = false
				end
			end
		end
		
		-- Keybinding to toggle ESP
		UserInputService.InputBegan:Connect(function(input, gameProcessed)
			if not gameProcessed and input.KeyCode == Enum.KeyCode.H then
				toggleESP()
			end
		end)
		
		-- Render ESP on each frame
		RunService.RenderStepped:Connect(function()
			if not skeletonEnabled then return end
		
			for _, player in ipairs(Players:GetPlayers()) do
				if player ~= Players.LocalPlayer and player.Character and player.Character:FindFirstChild("Humanoid") then
					local character = player.Character
					local humanoid = character:FindFirstChild("Humanoid")
					local head = character:FindFirstChild("Head")
					local nameTag = playerTags[player] or createNameTag()
		
					-- Create the name tag if it doesn't exist
					if not playerTags[player] then
						playerTags[player] = nameTag
					end
		
					if head and humanoid and humanoid.Health > 0 then
						-- Update the position and text of the name tag
						local screenPos = Camera:WorldToViewportPoint(head.Position)
						if screenPos.Z > 0 then
							nameTag.Position = Vector2.new(screenPos.X, screenPos.Y - 25) -- Above the head
							nameTag.Text = player.Name -- Set the username
							nameTag.Visible = true
						else
							nameTag.Visible = false
						end
		
						playerLines[player] = playerLines[player] or {}
						local lineIndex = 1
		
						-- Draw skeleton lines
						for _, connection in ipairs(bodyParts) do
							local part1 = character:FindFirstChild(connection[1])
							local part2 = character:FindFirstChild(connection[2])
		
							if part1 and part2 then
								local line = playerLines[player][lineIndex] or createLine()
								playerLines[player][lineIndex] = line
		
								local pos1, onScreen1 = Camera:WorldToViewportPoint(part1.Position)
								local pos2, onScreen2 = Camera:WorldToViewportPoint(part2.Position)
		
								if onScreen1 and onScreen2 then
									line.From = Vector2.new(pos1.X, pos1.Y)
									line.To = Vector2.new(pos2.X, pos2.Y)
									line.Visible = true
								else
									line.Visible = false
								end
							end
		
							lineIndex = lineIndex + 1
						end
		
						-- Hide unused lines for this player
						for i = lineIndex, #playerLines[player] do
							if playerLines[player][i] then
								playerLines[player][i].Visible = false
							end
						end
					else
						nameTag.Visible = false
					end
				elseif playerLines[player] then
					-- Hide lines and tags for players without valid characters
					for _, line in pairs(playerLines[player]) do
						line.Visible = false
					end
					if playerTags[player] then
						playerTags[player].Visible = false
					end
				end
			end
		end)
		
		-- Cleanup lines and tags when players leave
		Players.PlayerRemoving:Connect(function(player)
			if playerLines[player] then
				for _, line in pairs(playerLines[player]) do
					line:Remove()
				end
				playerLines[player] = nil
			end
			if playerTags[player] then
				playerTags[player]:Remove()
				playerTags[player] = nil
			end
		end)
    end,
})

local HitboxSection = MainTab:CreateSection({
   Name = "Hitbox"
})

-- Flag to track the toggle state
local isHitboxExpanded = false

-- Function to expand hitboxes
local function expandHitboxes()
    for _, player in pairs(game.Players:GetPlayers()) do
        if player.Character then
            for _, part in pairs(player.Character:GetChildren()) do
                if part:IsA("BasePart") then
                    part.Size = part.Size + Vector3.new(3, 3, 3) -- Adjust the increment as needed
                end
            end
        end
    end
end

-- Function to reset hitboxes to their original size
local function resetHitboxes()
    for _, player in pairs(game.Players:GetPlayers()) do
        if player.Character then
            for _, part in pairs(player.Character:GetChildren()) do
                if part:IsA("BasePart") then
                    part.Size = part.Size - Vector3.new(3, 3, 3) -- Ensure this matches the increment used in expandHitboxes
                end
            end
        end
    end
end

-- Adding the toggle to the FarmingSection
HitboxSection:AddToggle({
    Name = "Expand Hitboxes",
    Flag = "VisualsSection_ExpandHitboxes",
    Callback = function(state)
        isHitboxExpanded = state
        if isHitboxExpanded then
            expandHitboxes()
        else
            resetHitboxes()
        end
    end
})


 local SecondTab = PepsisWorld:CreateTab({
    Name = "Teleports"
    })

 local TpSection = SecondTab:CreateSection({
    Name = "Farming"
 })

 TpSection:AddButton({
    Name = "Blank Cards",
    Callback = function()
        local YourRoot = game:GetService("Players").LocalPlayer.Character.HumanoidRootPart
		--warn(YourRoot.CFrame)
		--local LocationToTP = workspace["El_belicorp"].HumanoidRootPart.CFrame
		local LocationToTP2 = CFrame.new(-464, 4, -579)
		YourRoot.CFrame = LocationToTP2
    end
 })

 TpSection:AddButton({
    Name = "Laptop 1",
    Callback = function()
        local YourRoot = game:GetService("Players").LocalPlayer.Character.HumanoidRootPart
		--warn(YourRoot.CFrame)
		--local LocationToTP = workspace["El_belicorp"].HumanoidRootPart.CFrame
		local LocationToTP2 = CFrame.new(27, 4, -560)
		YourRoot.CFrame = LocationToTP2
    end
 })
 

 TpSection:AddButton({
    Name = "Laptop 2",
    Callback = function()
        local YourRoot = game:GetService("Players").LocalPlayer.Character.HumanoidRootPart
		--warn(YourRoot.CFrame)
		--local LocationToTP = workspace["El_belicorp"].HumanoidRootPart.CFrame
		local LocationToTP2 = CFrame.new(160, 4, -420)
		YourRoot.CFrame = LocationToTP2
    end
 })

 TpSection:AddButton({
    Name = "Laptop 3",
    Callback = function()
        local YourRoot = game:GetService("Players").LocalPlayer.Character.HumanoidRootPart
		--warn(YourRoot.CFrame)
		--local LocationToTP = workspace["El_belicorp"].HumanoidRootPart.CFrame
		local LocationToTP2 = CFrame.new(-101, 4, -428)
		YourRoot.CFrame = LocationToTP2
    end
 })


 TpSection:AddButton({
    Name = "Laptop 4",
    Callback = function()
        local YourRoot = game:GetService("Players").LocalPlayer.Character.HumanoidRootPart
		--warn(YourRoot.CFrame)
		--local LocationToTP = workspace["El_belicorp"].HumanoidRootPart.CFrame
		local LocationToTP2 = CFrame.new(-49, 4, -428)
		YourRoot.CFrame = LocationToTP2
    end
 })


 TpSection:AddButton({
    Name = "Laptop 5",
    Callback = function()
        local YourRoot = game:GetService("Players").LocalPlayer.Character.HumanoidRootPart
		--warn(YourRoot.CFrame)
		--local LocationToTP = workspace["El_belicorp"].HumanoidRootPart.CFrame
		local LocationToTP2 = CFrame.new(4, 4, -428)
		YourRoot.CFrame = LocationToTP2
    end
 })

 TpSection:AddButton({
    Name = "Laptop 6",
    Callback = function()
        local YourRoot = game:GetService("Players").LocalPlayer.Character.HumanoidRootPart
		--warn(YourRoot.CFrame)
		--local LocationToTP = workspace["El_belicorp"].HumanoidRootPart.CFrame
		local LocationToTP2 = CFrame.new(57, 4, -429)
		YourRoot.CFrame = LocationToTP2
    end
 })

 TpSection:AddButton({
    Name = "Laptop 7",
    Callback = function()
        local YourRoot = game:GetService("Players").LocalPlayer.Character.HumanoidRootPart
		--warn(YourRoot.CFrame)
		--local LocationToTP = workspace["El_belicorp"].HumanoidRootPart.CFrame
		local LocationToTP2 = CFrame.new(-26, 4, -559)
		YourRoot.CFrame = LocationToTP2
    end
 })

 TpSection:AddButton({
    Name = "Laptop 8",
    Callback = function()
        local YourRoot = game:GetService("Players").LocalPlayer.Character.HumanoidRootPart
		--warn(YourRoot.CFrame)
		--local LocationToTP = workspace["El_belicorp"].HumanoidRootPart.CFrame
		local LocationToTP2 = CFrame.new(-78, 4, -560)
		YourRoot.CFrame = LocationToTP2
    end
 })

 TpSection:AddButton({
    Name = "Bank",
    Callback = function()
        local YourRoot = game:GetService("Players").LocalPlayer.Character.HumanoidRootPart
		--warn(YourRoot.CFrame)
		--local LocationToTP = workspace["El_belicorp"].HumanoidRootPart.CFrame
		local LocationToTP2 = CFrame.new(-466, 5, -4)
		YourRoot.CFrame = LocationToTP2
    end
 })

 local TpsSection = SecondTab:CreateSection({
    Name = "Teleports"
 })

 TpsSection:AddButton({
    Name = "Gunstore",
    Callback = function()
        local YourRoot = game:GetService("Players").LocalPlayer.Character.HumanoidRootPart
		--warn(YourRoot.CFrame)
		--local LocationToTP = workspace["El_belicorp"].HumanoidRootPart.CFrame
		local LocationToTP2 = CFrame.new(-157, 5, -118)
		YourRoot.CFrame = LocationToTP2
    end
 })

 TpsSection:AddButton({
    Name = "Sporting Goods",
    Callback = function()
        local YourRoot = game:GetService("Players").LocalPlayer.Character.HumanoidRootPart
		--warn(YourRoot.CFrame)
		--local LocationToTP = workspace["El_belicorp"].HumanoidRootPart.CFrame
		local LocationToTP2 = CFrame.new(-224, 4, -170)
		YourRoot.CFrame = LocationToTP2
    end
 })

 TpsSection:AddButton({
    Name = "Barbershop",
    Callback = function()
        local YourRoot = game:GetService("Players").LocalPlayer.Character.HumanoidRootPart
		--warn(YourRoot.CFrame)
		--local LocationToTP = workspace["El_belicorp"].HumanoidRootPart.CFrame
		local LocationToTP2 = CFrame.new(146, 4, -297)
		YourRoot.CFrame = LocationToTP2
    end
 })

 TpsSection:AddButton({
    Name = "Clothing Shop",
    Callback = function()
        local YourRoot = game:GetService("Players").LocalPlayer.Character.HumanoidRootPart
		--warn(YourRoot.CFrame)
		--local LocationToTP = workspace["El_belicorp"].HumanoidRootPart.CFrame
		local LocationToTP2 = CFrame.new(-653, 4, -426)
		YourRoot.CFrame = LocationToTP2
    end
 })

 TpsSection:AddButton({
    Name = "Shoe Store",
    Callback = function()
        local YourRoot = game:GetService("Players").LocalPlayer.Character.HumanoidRootPart
		--warn(YourRoot.CFrame)
		--local LocationToTP = workspace["El_belicorp"].HumanoidRootPart.CFrame
		local LocationToTP2 = CFrame.new(-630, 4, 9)
		YourRoot.CFrame = LocationToTP2
    end
 })

 TpsSection:AddButton({
    Name = "Tattoo Shop",
    Callback = function()
        local YourRoot = game:GetService("Players").LocalPlayer.Character.HumanoidRootPart
		--warn(YourRoot.CFrame)
		--local LocationToTP = workspace["El_belicorp"].HumanoidRootPart.CFrame
		local LocationToTP2 = CFrame.new(413, 4, -240)
		YourRoot.CFrame = LocationToTP2
    end
 })

 TpsSection:AddButton({
    Name = "Gym",
    Callback = function()
        local YourRoot = game:GetService("Players").LocalPlayer.Character.HumanoidRootPart
		--warn(YourRoot.CFrame)
		--local LocationToTP = workspace["El_belicorp"].HumanoidRootPart.CFrame
		local LocationToTP2 = CFrame.new(373, 4, -443)
		YourRoot.CFrame = LocationToTP2
    end
 })

 TpsSection:AddButton({
    Name = "Dealership",
    Callback = function()
        local YourRoot = game:GetService("Players").LocalPlayer.Character.HumanoidRootPart
		--warn(YourRoot.CFrame)
		--local LocationToTP = workspace["El_belicorp"].HumanoidRootPart.CFrame
		local LocationToTP2 = CFrame.new(239, 4, -513)
		YourRoot.CFrame = LocationToTP2
    end
 })

 TpsSection:AddButton({
    Name = "Deli",
    Callback = function()
        local YourRoot = game:GetService("Players").LocalPlayer.Character.HumanoidRootPart
		--warn(YourRoot.CFrame)
		--local LocationToTP = workspace["El_belicorp"].HumanoidRootPart.CFrame
		local LocationToTP2 = CFrame.new(34, 4, -20)
		YourRoot.CFrame = LocationToTP2
    end
 })

 local TpssSection = SecondTab:CreateSection({
    Name = "Turfs"
 })

 TpssSection:AddButton({
    Name = "CAB TURF",
    Callback = function()
        local YourRoot = game:GetService("Players").LocalPlayer.Character.HumanoidRootPart
		--warn(YourRoot.CFrame)
		--local LocationToTP = workspace["El_belicorp"].HumanoidRootPart.CFrame
		local LocationToTP2 = CFrame.new(246, -21, -129)
		YourRoot.CFrame = LocationToTP2
    end
 })

 TpssSection:AddButton({
    Name = "ATC TURF",
    Callback = function()
        local YourRoot = game:GetService("Players").LocalPlayer.Character.HumanoidRootPart
		--warn(YourRoot.CFrame)
		--local LocationToTP = workspace["El_belicorp"].HumanoidRootPart.CFrame
		local LocationToTP2 = CFrame.new(308, -45, 51)
		YourRoot.CFrame = LocationToTP2
    end
 })

 TpssSection:AddButton({
    Name = "OTK TURF",
    Callback = function()
        local YourRoot = game:GetService("Players").LocalPlayer.Character.HumanoidRootPart
		--warn(YourRoot.CFrame)
		--local LocationToTP = workspace["El_belicorp"].HumanoidRootPart.CFrame
		local LocationToTP2 = CFrame.new(-150, -47, 149)
		YourRoot.CFrame = LocationToTP2
    end
 })

 TpssSection:AddButton({
    Name = "AS TURF",
    Callback = function()
        local YourRoot = game:GetService("Players").LocalPlayer.Character.HumanoidRootPart
		--warn(YourRoot.CFrame)
		--local LocationToTP = workspace["El_belicorp"].HumanoidRootPart.CFrame
		local LocationToTP2 = CFrame.new(-234, -47, 153)
		YourRoot.CFrame = LocationToTP2
    end
 })

 TpssSection:AddButton({
    Name = "DOA TURF",
    Callback = function()
        local YourRoot = game:GetService("Players").LocalPlayer.Character.HumanoidRootPart
		--warn(YourRoot.CFrame)
		--local LocationToTP = workspace["El_belicorp"].HumanoidRootPart.CFrame
		local LocationToTP2 = CFrame.new(-525, -37, -112)
		YourRoot.CFrame = LocationToTP2
    end
 })

 TpssSection:AddButton({
    Name = "CSM TURF",
    Callback = function()
        local YourRoot = game:GetService("Players").LocalPlayer.Character.HumanoidRootPart
		--warn(YourRoot.CFrame)
		--local LocationToTP = workspace["El_belicorp"].HumanoidRootPart.CFrame
		local LocationToTP2 = CFrame.new(-693, -37, 85)
		YourRoot.CFrame = LocationToTP2
    end
 })
